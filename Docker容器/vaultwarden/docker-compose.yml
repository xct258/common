services:
  vaultwarden:
    image: vaultwarden/server:latest
    container_name: vaultwarden
    restart: always
    ports:
      - "127.0.0.1:8808:80"
    environment:
      - SIGNUPS_ALLOWED=false
    volumes:
      - ./data/:/data/